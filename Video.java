package com.varsha.hint_30;

import android.app.Activity;
import android.net.Uri;
import android.os.Bundle;
import android.support.annotation.Nullable;
import android.widget.MediaController;
import android.widget.VideoView;

/**
 * Created by VARSHANI on 14-03-2018.
 */

public class Video extends Activity
{
   /* VideoView videoView;
    String videoUrl = "https://www.youtube.com/watch?v=_FtKzbGX5jk";


    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.fragment_fragment1);
        videoView = (VideoView) findViewById(R.id.videoview);
        videoView.setVideoURI(Uri.parse(videoUrl));
        videoView.setMediaController(new MediaController(Video.this));
        videoView.start();
    }
    */




}
